# swift-stakeholder AGENTS

1. Use Swift Package Manager only.
2. Target cross-platform CLI (`swift run`, `swift test`).
3. Use `core/` as submodule.
4. Implement parity contract and deterministic fixtures first.
5. No Xcode dependency.
6. Document all intentional differences and test with deterministic seeds.
